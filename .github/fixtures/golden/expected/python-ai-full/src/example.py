"""Placeholder module so the scaffold passes the quality gate from the first run.

Delete this and `tests/test_example.py` when you build your first real slice.
Real code follows the two-layer split in `docs/structure.txt` (`src/backend`,
`src/frontend`) and the rules in `AGENTS.md` `<architecture-discipline>`.
"""

from __future__ import annotations


def greet(name: str) -> str:
    """Return a friendly greeting for ``name``."""
    return f"Hello, {name}!"
